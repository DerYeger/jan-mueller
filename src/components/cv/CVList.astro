---
interface Props {
  id: string
}

const { id } = Astro.props
---

<div class="flex flex-col gap-8 select-text" data-testid={id}>
  <h2 class="title text-3xl scroll-mt-20" id={id}>
    <slot name="title" />
  </h2>
  <slot />
</div>
