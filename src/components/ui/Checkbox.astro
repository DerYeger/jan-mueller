---
import { cn } from '@components/ui/cn'

interface Props extends Omit<astroHTML.JSX.InputHTMLAttributes, 'type'> {
}

const { class: className, ...props } = Astro.props
---

<input
  type="checkbox"
  class={cn(`appearance-none rounded-sm bg-base transition-colors checked:bg-blue-500 checked:border-blue-600 border border-medium size-4 disabled:bg-disabled relative checked:before:content-['✓'] checked:before:absolute
         checked:before:inset-0 checked:before:flex
         checked:before:items-center checked:before:justify-center
         checked:before:text-white checked:before:text-sm
         checked:before:font-bold focus-visible:focus-ring`, className)}
  {...props} />
